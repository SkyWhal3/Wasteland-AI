---
name: Bug report
about: You ran it, it broke — the report that gets it fixed
title: "[script name]: one-line summary"
labels: bug
---

<!-- "It didn't work" is a mood, not a bug report. This template is the
     difference between a fix tonight and twenty questions first. -->

**Script / doc involved:**

**OS + Python version** (`python --version`):

**Exact command you ran:**

```
paste it here
```

**Full output / traceback:**

```
paste it here — all of it, not the last line
```

**If `?med` / kiwix related:** kiwix-tools version, and the book name shown
at http://localhost:8080:

**If serial related:** paste `python power_monitor.py --list-ports` output:

**What you expected instead:**
